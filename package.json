{
  "name": "random-array-ensure",
  "version": "1.0.5",
  "description": "Retrieve random elements from an array, ensuring that al least each item will be received one time and that no duplicates will be received after each array loop.",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "watch": "npx tsc --watch",
    "build": "npx tsc --build",
    "test": "node ./lib/test.js"
  },
  "files": [
    "lib/",
    "README.md"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Llorx/random-array-ensure"
  },
  "keywords": [
    "random",
    "randomize",
    "array",
    "shuffle",
    "ensure",
    "duplicates",
    "check"
  ],
  "author": "Llorx",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.5.2"
  }
}
